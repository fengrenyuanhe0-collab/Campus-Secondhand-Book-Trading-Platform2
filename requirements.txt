# 核心 Django / Core Django
Django>=4.2,<5.0
djangorestframework>=3.14

# 图片处理 / Image handling
Pillow>=10.0

# PostgreSQL 驱动 / PostgreSQL driver
psycopg2-binary>=2.9

# Redis 缓存 / Redis cache (falls back to in-memory if unavailable)
redis>=5.0
django-redis>=5.4

# WSGI 服务器（生产）/ Production WSGI server
gunicorn>=21.2
